# CloudLight Blizzard 2.0.5

本版本新增并强化《守望先锋》智能区服差异备份，以更小的本地占用完成国服与国际服文件切换，并对单文件异常提供安全容错。

## 智能差异备份

- 新增当前区服 → 另一区服 → 当前区服的 A1 → B1 → A2 往返验证流程，只保存确认稳定的区服差异文件。
- 区分已确认差异、非稳定变化和文件异常；游戏运行产生的缓存、日志或临时变化会自动忽略。
- Candidate 读取、Hash、复制或校验失败时只跳过对应文件，其他候选文件仍可继续完成准备。
- 最终 Generation 只声明两侧备份完整且通过 size/Hash 校验的 Entry，避免 manifest 引用缺失或损坏内容。
- 准备完成时显示已确认、自动忽略和异常跳过数量，并可展开查看异常摘要。
- Candidate 存在但最终没有任何可用 Entry 时不会激活空 Generation，旧 Active Generation 保持不变。

## 区服切换可靠性

- VerifiedDifference 日常恢复改为逐文件容错，单个备份缺失、Hash 错误、文件占用、删除或替换失败不会阻止其他独立文件。
- 切换结果区分成功、部分成功和失败；部分成功会显示警告并保留详细日志。
- 文件恢复先写入临时文件并校验，再原子替换目标，失败时保留原文件，避免留下半写入内容。
- FullSnapshot 模式保持兼容，可在智能差异备份不适用时继续使用。

## 安装

- 适用于 Windows x64。
- 需要 .NET 8 Windows Desktop Runtime x64；未安装时安装程序会提供官方下载入口。

**完整变更**：https://github.com/yundan125/CloudLight-Blizzard/compare/v2.0.4...v2.0.5
