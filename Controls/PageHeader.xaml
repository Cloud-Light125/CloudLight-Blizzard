<UserControl x:Class="CloudLightBlizzard.Controls.PageHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="clr-namespace:CloudLightBlizzard.Controls"
             Background="Transparent">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel VerticalAlignment="Top">
            <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type ctrl:PageHeader}}}"
                       Style="{StaticResource PageTitle}"/>
            <TextBlock Text="{Binding Description, RelativeSource={RelativeSource AncestorType={x:Type ctrl:PageHeader}}}"
                       Style="{StaticResource PageDescription}"/>
        </StackPanel>
        <ContentPresenter Grid.Column="1" Content="{Binding HeaderActions,
                              RelativeSource={RelativeSource AncestorType={x:Type ctrl:PageHeader}}}"
                          VerticalAlignment="Top"/>
        <ctrl:AnnouncementBell Grid.Column="2" Margin="12,0,0,0" VerticalAlignment="Top"/>
    </Grid>
</UserControl>
