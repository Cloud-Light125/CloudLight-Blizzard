<UserControl x:Class="BnetSwitch.Views.Pages.StatsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Foreground="{DynamicResource Text.Primary}" Background="{DynamicResource App.Background}">
    <Grid Margin="28,24,22,22">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="战绩" Style="{StaticResource PageTitle}"/>
            <TextBlock Text="查看已保存账号的守望先锋生涯与比赛数据。" Style="{StaticResource PageDescription}"/>
        </StackPanel>
        <Border Grid.Row="1" Style="{StaticResource SurfaceCard}" Padding="16" Margin="0,20,0,16">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="330"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <TextBlock Text="账号" VerticalAlignment="Center" Foreground="{DynamicResource Text.Secondary}" Margin="0,0,10,0"/>
                <ComboBox x:Name="AccountBox" Grid.Column="1" Style="{StaticResource ThemeComboBox}" DisplayMemberPath="DisplayName" SelectionChanged="OnAccountChanged"/>
                <TextBlock Grid.Column="2" Text="区服" VerticalAlignment="Center" Foreground="{DynamicResource Text.Secondary}" Margin="20,0,10,0"/>
                <StackPanel Grid.Column="3" VerticalAlignment="Center">
                    <TextBlock x:Name="RegionText" FontWeight="SemiBold"/>
                    <TextBlock x:Name="BattleTagText" Foreground="{DynamicResource Text.Muted}" FontSize="11"/>
                </StackPanel>
                <Button Grid.Column="4" Content="刷新" Style="{StaticResource SecondaryButton}" Padding="16,0" Click="OnRefresh"/>
            </Grid>
        </Border>
        <Border Grid.Row="2" Background="{DynamicResource App.Surface}" BorderBrush="{DynamicResource App.Border}" BorderThickness="1" CornerRadius="12" ClipToBounds="True">
            <Grid>
                <ContentControl x:Name="StatsHost"/>
                <StackPanel x:Name="EmptyPanel" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="选择一个已保存账号" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Text="国服账号显示国服战绩，国际服账号显示暴雪公开生涯。" Foreground="{DynamicResource Text.Secondary}" Margin="0,7,0,0"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
