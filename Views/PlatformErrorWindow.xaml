<Window x:Class="CloudLightBlizzard.Views.PlatformErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="服务运行失败" Width="440" SizeToContent="Height" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False" Background="{DynamicResource App.Background}"
        Foreground="{DynamicResource Text.Primary}">
    <Border Padding="22">
        <StackPanel>
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" LineHeight="22"/>
            <WrapPanel HorizontalAlignment="Right" Margin="0,22,0,0">
                <Button Content="查看日志" Style="{StaticResource PrimaryButton}" Padding="16,0" Click="OnViewLogs"/>
                <Button Content="关闭" Style="{StaticResource SecondaryButton}" Padding="16,0" Margin="8,0,0,0" IsCancel="True"/>
            </WrapPanel>
        </StackPanel>
    </Border>
</Window>
