<Window x:Class="CloudLightBlizzard.Views.AnnouncementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="公告" Width="720" Height="520" MinWidth="620" MinHeight="420"
        WindowStartupLocation="CenterOwner" Background="{DynamicResource App.Background}"
        Foreground="{DynamicResource Text.Primary}" ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="公告" Style="{StaticResource PageTitle}"/>
        </StackPanel>
        <Grid Grid.Row="1" Margin="0,20,0,18">
            <Grid.ColumnDefinitions><ColumnDefinition Width="260"/><ColumnDefinition Width="18"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <Border Style="{StaticResource SurfaceCard}" Padding="0">
                <Grid>
                    <ListBox x:Name="AnnouncementList" Background="Transparent" BorderThickness="0"
                             SelectionChanged="OnAnnouncementSelected">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="10,9">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <Ellipse Width="7" Height="7" Fill="{DynamicResource Accent.Primary}"
                                             Visibility="{Binding UnreadVisibility}" VerticalAlignment="Top" Margin="0,5,0,0"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Announcement.Title}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                        <TextBlock Text="{Binding PublishedText}" Foreground="{DynamicResource Text.Muted}" FontSize="11" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <TextBlock x:Name="EmptyHint" Text="暂无可用公告" Foreground="{DynamicResource Text.Muted}"
                               HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                </Grid>
            </Border>
            <Border Grid.Column="2" Style="{StaticResource SurfaceCard}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock x:Name="DetailHint" Text="请选择一条公告查看正文" Foreground="{DynamicResource Text.Muted}"/>
                        <TextBlock x:Name="DetailTitle" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap" Visibility="Collapsed"/>
                        <TextBlock x:Name="DetailTime" Foreground="{DynamicResource Text.Muted}" Margin="0,7,0,18" Visibility="Collapsed"/>
                        <TextBlock x:Name="DetailContent" TextWrapping="Wrap" LineHeight="24" Visibility="Collapsed"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
        <Button Grid.Row="2" Content="关闭" HorizontalAlignment="Right" Style="{StaticResource SecondaryButton}"
                Padding="18,0" Click="OnClose"/>
    </Grid>
</Window>
