<Window x:Class="CloudLightBlizzard.Views.SwitchPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="切换前预览" WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        AllowsTransparency="True" Background="Transparent" WindowStartupLocation="CenterOwner"
        Width="560" SizeToContent="Height" FontFamily="Microsoft YaHei UI" FontSize="13">
    <Border Background="{DynamicResource App.SurfaceElevated}" CornerRadius="14" Margin="18" BorderBrush="{DynamicResource App.Border}" BorderThickness="1">
        <Border.Effect><DropShadowEffect BlurRadius="30" ShadowDepth="0" Opacity="0.36" Color="#000000"/></Border.Effect>
        <StackPanel>
            <Grid Margin="22,18,18,0" MouseLeftButtonDown="OnDrag"><TextBlock Text="切换前预览" FontSize="19" FontWeight="SemiBold"/><Button Style="{StaticResource TitleIconButton}" HorizontalAlignment="Right" Width="28" Height="28" Content="×" Click="OnCancel"/></Grid>
            <StackPanel Margin="22,17,22,0">
                <TextBlock x:Name="RouteText" FontSize="16" FontWeight="SemiBold"/>
                <Border Background="{DynamicResource App.SurfaceAlt}" CornerRadius="9" Padding="14,12" Margin="0,12,0,0"><UniformGrid Columns="5"><StackPanel><TextBlock Text="恢复" Foreground="{DynamicResource Text.Muted}"/><TextBlock x:Name="RestoreText" FontSize="18" FontWeight="SemiBold"/></StackPanel><StackPanel><TextBlock Text="覆盖" Foreground="{DynamicResource Text.Muted}"/><TextBlock x:Name="OverwriteText" FontSize="18" FontWeight="SemiBold"/></StackPanel><StackPanel><TextBlock Text="删除" Foreground="{DynamicResource Text.Muted}"/><TextBlock x:Name="DeleteText" FontSize="18" FontWeight="SemiBold"/></StackPanel><StackPanel><TextBlock Text="保持" Foreground="{DynamicResource Text.Muted}"/><TextBlock x:Name="KeepText" FontSize="18" FontWeight="SemiBold"/></StackPanel><StackPanel><TextBlock Text="预计写入" Foreground="{DynamicResource Text.Muted}"/><TextBlock x:Name="BytesText" FontSize="18" FontWeight="SemiBold"/></StackPanel></UniformGrid></Border>
                <TextBlock x:Name="StateText" Margin="0,12,0,0" TextWrapping="Wrap"/>
                <TextBlock x:Name="SnapshotText" Foreground="{DynamicResource Text.Secondary}" Margin="0,5,0,0" TextWrapping="Wrap"/>
                <Border x:Name="WarningBox" Background="{DynamicResource WarnBg}" BorderBrush="{DynamicResource WarnBd}" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,12,0,0" Visibility="Collapsed"><StackPanel><TextBlock Text="安全检查提示" Foreground="{DynamicResource WarnTx}" FontWeight="SemiBold"/><TextBlock x:Name="WarningText" Foreground="{DynamicResource WarnTx}" TextWrapping="Wrap" LineHeight="20" Margin="0,5,0,0"/></StackPanel></Border>
                <Border x:Name="BlockerBox" Background="{DynamicResource WarnBg}" BorderBrush="{DynamicResource Status.Error}" BorderThickness="1" CornerRadius="8" Padding="12,10" Margin="0,12,0,0" Visibility="Collapsed"><StackPanel><TextBlock Text="已阻止切换" Foreground="{DynamicResource Status.Error}" FontWeight="SemiBold"/><TextBlock x:Name="BlockerText" Foreground="{DynamicResource Status.Error}" TextWrapping="Wrap" LineHeight="20" Margin="0,5,0,0"/></StackPanel></Border>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="22,19,22,22"><Button Content="取消" Style="{StaticResource SecondaryButton}" Height="36" Padding="18,0" Margin="0,0,10,0" Click="OnCancel"/><Button x:Name="StartButton" Content="开始切换" Style="{StaticResource PrimaryButton}" Height="36" Padding="18,0" Click="OnStart"/></StackPanel>
        </StackPanel>
    </Border>
</Window>
